import { AppShell } from "@/components/AppShell";
import { getAtlasContext } from "@/lib/context";
import { atlasRepository } from "@/lib/repository";
export const dynamic="force-dynamic";
export default async function TrustPage(){const context=await getAtlasContext();const decisions=await atlasRepository.decisions(context.organization.id,context.workspace.id);return <AppShell><p className="eyebrow">Trust Fabric</p><h1>Evidence before execution</h1><p className="muted">{context.organization.name} · {context.workspace.name} · Every decision carries actor and role attribution.</p><article className="card table-wrap"><table className="table"><thead><tr><th>Decision</th><th>Action</th><th>Actor</th><th>Policy</th><th>Risk</th><th>Evidence</th></tr></thead><tbody>{decisions.length?decisions.map((item)=><tr key={item.id}><td><span className={`badge ${item.decision==="ALLOW"?"good":"warn"}`}>{item.decision}</span></td><td>{item.action}</td><td>{item.actorRole}</td><td>{item.policyId}</td><td>{Math.round(item.riskScore*100)}%</td><td className="muted">{item.evidence.join(" · ")}</td></tr>):<tr><td colSpan={6} className="muted">No trust decisions in this workspace.</td></tr>}</tbody></table></article></AppShell>}
