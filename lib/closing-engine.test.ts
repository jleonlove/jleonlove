import {describe,expect,it} from 'vitest'; import {applyDealEvent,closingGate,nextClosingActions} from './closing-engine'; import type {AtlasDeal} from './types';
const base:AtlasDeal={id:'d',organizationId:'o',workspaceId:'w',createdBy:'u',title:'Gold',commodity:'gold',quantity:200,unit:'kg',stage:'qualification',participants:[{id:'s',name:'Seller',role:'seller',authority:'CLAIMED'}],evidence:[{id:'e',claim:'product control',status:'CLAIMED',sourceIds:['s'],independentSourceGroups:1,observedAt:'2026-08-14'}],requirements:[{id:'a',title:'Verify authority',status:'OPEN',severity:'CRITICAL',ownerRole:'seller',dependsOn:[],evidenceIds:[]},{id:'c',title:'Contract',status:'OPEN',severity:'HIGH',ownerRole:'legal',dependsOn:['a'],evidenceIds:[]}],assumptions:[{id:'x',statement:'200kg logistics',dependencyKeys:['quantity'],valid:true}],createdAt:'2026-08-14',updatedAt:'2026-08-14'};
describe('closing engine',()=>{it('fails closed',()=>expect(closingGate(base).pass).toBe(false));it('is idempotent and invalidates assumptions',()=>{const seen=new Set<string>(),ev={id:'1',dealId:'d',type:'DEAL_CHANGED',occurredAt:'2026-08-15',payload:{quantity:500},idempotencyKey:'k'};const one=applyDealEvent(base,ev,seen);expect(one.deal.quantity).toBe(500);expect(one.deal.assumptions[0].valid).toBe(false);expect(applyDealEvent(one.deal,ev,seen).duplicate).toBe(true)});it('returns prioritized actions',()=>expect(nextClosingActions(base)[0].severity).toBe('HIGH'));});
