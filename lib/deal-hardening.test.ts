import { describe, expect, it } from "vitest";
import { computeCriticalPath, evaluatePhysicalReality, invalidateAssumptions, verifyAuthorityChain } from "./deal-hardening";
import type { AtlasDeal } from "./types";
const deal:AtlasDeal={id:"d",organizationId:"o",workspaceId:"w",createdBy:"u",title:"Gold",commodity:"gold",stage:"qualification",participants:[{id:"seller",name:"Seller",role:"seller",authority:"CLAIMED"}],evidence:[{id:"e1",claim:"capacity",status:"DOCUMENT_SUPPORTED",sourceIds:["seller"],independentSourceGroups:1,observedAt:"2026-08-14"}],requirements:[{id:"auth",title:"Authority",status:"OPEN",severity:"CRITICAL",ownerRole:"seller",dependsOn:[],evidenceIds:[]},{id:"contract",title:"Contract",status:"OPEN",severity:"HIGH",ownerRole:"legal",dependsOn:["auth"],evidenceIds:[]}],assumptions:[{id:"a",statement:"200kg feasible",dependencyKeys:["quantity"],valid:true}],createdAt:"2026-08-14",updatedAt:"2026-08-14"};
describe("deal hardening",()=>{it("fails unverified authority",()=>expect(verifyAuthorityChain(deal,[{id:"x",fromParticipantId:"seller",toParticipantId:"seller",scope:["sell"],status:"CLAIMED",evidenceIds:[]}]).valid).toBe(false));it("detects physical conflict",()=>expect(evaluatePhysicalReality([{id:"a",metric:"capacity",value:200,unit:"kg",sourceEvidenceIds:["e1"]},{id:"b",metric:"capacity",value:500,unit:"kg",sourceEvidenceIds:["e1"]}],deal.evidence)[0].severity).toBe("CRITICAL"));it("prioritizes dependent critical path",()=>expect(computeCriticalPath(deal).ordered.length).toBe(2));it("invalidates changed assumptions",()=>expect(invalidateAssumptions(deal,["quantity"])[0].valid).toBe(false));});
