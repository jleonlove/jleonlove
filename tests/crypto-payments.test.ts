import {describe,it,expect} from "vitest"; import {validatePaymentIntent,reconcilePayment,requiresTreasuryApproval,type CryptoPaymentIntent} from "../lib/crypto-payments";
const base=(asset:any,network:string,memoOrTag?:string):CryptoPaymentIntent=>({id:"p1",invoiceId:"i1",organizationId:"o1",workspaceId:"w1",asset,network,expectedAtomic:"100",destination:"dest",memoOrTag,quoteExpiresAt:"2099-01-01T00:00:00Z",state:"QUOTE_LOCKED",createdAt:new Date().toISOString()});
describe("digital asset payments",()=>{it("supports XLM memo policy",()=>{expect(validatePaymentIntent(base("XLM","stellar-public")).ok).toBe(false);expect(validatePaymentIntent(base("XLM","stellar-public","invoice-1")).ok).toBe(true)});it("rejects wrong network",()=>expect(validatePaymentIntent(base("BTC","ethereum-mainnet")).errors).toContain("WRONG_NETWORK"));it("detects underpayment",()=>expect(reconcilePayment(base("ETH","ethereum-mainnet"),{asset:"ETH",network:"ethereum-mainnet",txHash:"0x1",receivedAtomic:"99"}).state).toBe("UNDERPAID"));it("separates treasury authority",()=>{expect(requiresTreasuryApproval("RECEIVE")).toBe(false);expect(requiresTreasuryApproval("WITHDRAW")).toBe(true)})});
